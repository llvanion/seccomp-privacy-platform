{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "api_identity_token_map/v1",
  "title": "Local API bearer token to caller identity mapping",
  "type": "object",
  "required": ["schema", "tokens"],
  "properties": {
    "schema": {
      "const": "api_identity_token_map/v1"
    },
    "jwt_bearer": {
      "type": "object",
      "properties": {
        "issuer": { "type": "string" },
        "claim_mapping_config": { "type": "string", "minLength": 1 },
        "verify_secret_env": { "type": "string", "minLength": 1 },
        "jwks_uri": { "type": "string", "minLength": 1 },
        "trusted_audiences": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "require_registered_issuer": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "tokens": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["token_env", "issuer", "subject"],
        "properties": {
          "token_env": { "type": "string", "minLength": 1 },
          "issuer": { "type": "string" },
          "subject": { "type": "string", "minLength": 1 },
          "description": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
