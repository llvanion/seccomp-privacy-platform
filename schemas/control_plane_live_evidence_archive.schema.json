{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "control_plane_live_evidence_archive/v1",
  "title": "Control-plane live evidence archive",
  "type": "object",
  "required": [
    "schema",
    "generated_at_utc",
    "status",
    "job_id",
    "live_artifact_count",
    "live_artifacts",
    "findings",
    "artifacts",
    "aggregate_sha256"
  ],
  "properties": {
    "schema": { "const": "control_plane_live_evidence_archive/v1" },
    "generated_at_utc": { "type": "string", "minLength": 1 },
    "status": { "enum": ["ok", "fail"] },
    "job_id": { "type": "string", "minLength": 1 },
    "live_artifact_count": { "type": "integer", "minimum": 0 },
    "live_artifacts": {
      "type": "object",
      "required": [
        "live_repo_side_control_plane_foundation",
        "live_operator_runbook_report",
        "live_metadata_api_report",
        "live_platform_api_report",
        "live_reverse_proxy_report"
      ],
      "properties": {
        "live_repo_side_control_plane_foundation": {},
        "live_operator_runbook_report": {},
        "live_metadata_api_report": {},
        "live_platform_api_report": {},
        "live_reverse_proxy_report": {}
      },
      "additionalProperties": false
    },
    "findings": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "size_bytes", "sha256"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "size_bytes": { "type": "integer", "minimum": 0 },
          "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
        },
        "additionalProperties": false
      }
    },
    "aggregate_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
  },
  "additionalProperties": false
}
