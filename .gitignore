runs/
tmp/
*.log
*.tar.gz
*.zip
*.deb
__pycache__/
*.pyc
.DS_Store

# Shared workspace artifacts
1.pdf

# a-psi artifacts
a-psi/data/raw/
a-psi/data/extracted/
a-psi/runs/
a-psi/logs/

# sse artifacts
sse/.sse/
sse/.pytest_cache/
sse/__pycache__/
sse/.venv/

# Rust/Cargo artifacts
target/
bridge/target/

# Local release / build outputs
dist/
build/
console/dist/
console/node_modules/
console/.vite/

# Bridge job outputs (run artifacts, not source)
bridge/out/

# Submission documents (contains personal info / student forms — do not push)
submit documents/

# LaTeX intermediates + generated PDFs in report/
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.synctex.gz
report/seccomp_privacy_platform_report.pdf
report/大创.pdf
report/项目报告.pdf
report/结题报告.pdf
report/结题报告+(1).pdf

# Operator-console node tooling
node_modules/
.vite/
.cache/
